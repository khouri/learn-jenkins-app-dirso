pipeline {
    agent any

    stages {
        stage('without docker') {
            steps {
                echo 'Hello World'
                sh 'echo "without docker" '
            }
        }
    }
    
}
